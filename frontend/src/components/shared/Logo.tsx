import { Link } from "react-router-dom";
import Typography from "@mui/material/Typography";

const Logo = () => {
  return (
    <div
      style={{
        display: "flex",
        marginRight: "20px",
        alignItems: "center",
        gap: "15px", // Keep the gap to maintain space
      }}
    >
      
    </div>
  );
};

export default Logo;
